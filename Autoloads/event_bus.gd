extends Node

signal window_size_changed(index) # emitted by options_menu.gd and received by user_config.gd
signal window_mode_toggled(toggle_state) # emitted by options_menu.gd and received by user_config.gd
